# Backend/Auth
BETTER_AUTH_SECRET=replace-with-random-secret
BETTER_AUTH_URL=http://localhost:3000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/corefansv0

# CORS: frontend origin allowed by the auth server
CLIENT_ORIGIN=http://localhost:5173

# Optional: override auth server port (defaults to 3000)
# PORT=3000

# Frontend
VITE_AUTH_BASE_URL=http://localhost:3000

# Streaming (encoder + playback)
# RTMP ingress base used by the server to hint encoder settings in /api/stream/start
# HLS playback base used by the server/client to form playback URLs
RTMP_BASE_URL=rtmp://localhost:1935/live
HLS_BASE_URL=http://localhost:8080/hls
VITE_HLS_BASE_URL=http://localhost:8080/hls

# Realtime WebSocket URL (for viewer counts/chat)
# VITE_WS_URL=ws://localhost:3000/ws

# Cookies & Security (Better Auth)
# In production, if auth and app are on different domains, cookies must be cross-site safe.
## Recommended production defaults
# COOKIE_SAMESITE=none
# COOKIE_SECURE=true
# COOKIE_PARTITIONED=true
# COOKIE_DOMAIN=.example.com  # enables cross-subdomain cookies (e.g., auth.example.com + app.example.com)

# CORS / Trusted Origins
# Comma-separated list of allowed origins for the auth server and Better Auth trustedOrigins.
# TRUSTED_ORIGINS overrides CLIENT_ORIGIN when present.
# TRUSTED_ORIGINS=https://app.example.com,https://auth.example.com

## Local defaults (usually fine):
# COOKIE_SAMESITE=lax
# COOKIE_SECURE=false
# COOKIE_PARTITIONED=false
# COOKIE_DOMAIN=
# TRUSTED_ORIGINS=http://localhost:5173

# MinIO (CMS)
MINIO_ENDPOINT=127.0.0.1
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET_VIDEOS=videos
MINIO_BUCKET_THUMBS=thumbs

# Indexer / Chain
# Defaults to Core Testnet2 if not provided
CHAIN_ID=1114
CHAIN_RPC_URL=https://rpc.test2.btcs.network
# Contract addresses to watch (required for indexer to start)
SUBSCRIPTION_MANAGER_ADDRESS=0xYourSubscriptionManager
ACCESS_PASS_ADDRESS=0xYourAccessPass
CREATOR_REGISTRY_ADDRESS=0xYourCreatorRegistry
INDEXER_START_BLOCK=

# Dev seeding (server-only)
# Enable dev-only endpoints for local chain seeding. Keep disabled in production.
DEV_SEED_ENABLE=false
# Optional shared secret header to protect dev seed endpoints
# When set, requests must include:  x-dev-seed-key: $DEV_SEED_KEY
DEV_SEED_KEY=
# Optional default private key used by seeding endpoints when request body omits it
# For Anvil, you can paste one of the pre-funded accounts' private keys
DEV_WALLET_PRIVATE_KEY=

# Frontend (Vite)
VITE_CHAIN_ID=1114
VITE_CHAIN_RPC_URL=https://rpc.test2.btcs.network
VITE_SUBSCRIPTION_MANAGER_ADDRESS=0xYourSubscriptionManager
# Address of the CreatorRegistry contract (for settings UI)
VITE_CREATOR_REGISTRY_ADDRESS=0xYourCreatorRegistry
# Optional: dev seeding key forwarded as `x-dev-seed-key` header by the frontend
VITE_DEV_SEED_KEY=
# Demo creator address for local/testing (must exist in CreatorRegistry)
VITE_DEMO_CREATOR_ADDRESS=0x000000000000000000000000000000000000dEaD

# ---
# End of example
#
